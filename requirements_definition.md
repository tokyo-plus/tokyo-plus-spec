# TOKYO+ 要件定義書

## 1. ドキュメントの目的

本書は、TOKYO+ のMVP開発に向けて、確定した要件、将来構想、デモおよび受入条件を整理するための要件定義書である。

TOKYO+ は、都市に分散しているオープンデータや外部データをAIが横断的に統合し、利用者に必要な「情報」ではなく「判断」を届ける都市AIエージェントを目指す。

本書は、今後の基本設計、詳細設計、実装の前提資料として利用する。

## 2. プロジェクト概要

### 2.1 プロダクト名

TOKYO+

### 2.2 プロダクトコンセプト

人が都市を理解するのではなく、都市が人を理解する。

ただし、MVPにおける実装上の中心は「ユーザー理解」ではなく「都市理解」とする。iOSネイティブアプリで取得できる現在地、設定画面で選択したユーザータグ、東京都オープンデータ、外部APIをもとに、AIが行動判断を生成する。アプリ起動後の自由入力はMVPでは扱わない。

### 2.3 目指す価値

TOKYO+ は、複数のサービスやデータソースを利用者が自分で横断して判断する負担を減らし、利用者が今どう行動すべきかを短く、理由付きで提示する。

従来の都市サービスが情報を検索・表示するものであるのに対し、TOKYO+ は都市データを統合し、行動の優先順位を判断する「都市の判断レイヤー」として機能する。

### 2.4 将来の目指す姿

TOKYO+は、機能を一つずつ追加していくことで、単に構造的に機能を充実させるサービスではない。オープンデータや外部データを横断的に活用し、一人ひとりにパーソナライズされた判断を生成する「都市AIエージェント」への進化を目指す。

そのために、ユーザーの属性や関心、生活スタイルなどをタグとして捉え、「どのような人か」をAIが理解できるユーザーコンテキストとして設定する。AIはそのコンテキストをもとに、ユーザーに関係のあるデータをオープンデータや外部データから探索・取得し、必要な判断と推奨行動を生成する。レスポンスには、何をすべきかだけでなく、その理由や判断に用いた参照元も添えることで、利用者が納得して行動できる状態をつくる。

一方で、ハッカソンの限られた開発期間で、この将来像のすべてを実装することは難しい。そこで今回は、将来の拡張につながるMVPとして、対象機能を「今日のアドバイス」に絞る。出力形式、説明責任、データ鮮度などの共通ルールは固定するが、AIエージェントが参照するデータや判断シナリオは1種類に限定せず、実データで検証できる複数のシナリオパックを実現する。避難先の選定など安全性の高い機能は、MVPでは独自判断を避けた注意喚起に限定する。

## 3. 開発スコープ

### 3.1 MVPの対象

本MVPでは、以下の機能を開発対象とする。

1. 今日のアドバイス

### 3.2 MVPで重視すること

- 複数データを統合して、利用者に1つの判断を返すこと
- 判断には必ず理由を添えること
- 情報量を増やさず、行動に直結する提案を優先すること
- UIはダッシュボード型ではなく、判断を中心にした最小構成とすること
- ハッカソン向けに、完成度よりもコンセプトの実証を優先すること
- ハッカソンでは、東京都オープンデータを効率的に活用して機能として成立させることを優先すること
- AIに任せる判断の考え方をルールとして明確にし、開発チームが検証可能な範囲に限定すること
- 実データを用いた一連の体験を成立させること
- 暑さ、天候変化、外出先通信、工事、地震情報確認など複数のシナリオを、共通の今日のアドバイス形式で成立させること

### 3.3 MVPで対象外とすること

以下は将来構想として保持するが、MVPでは原則として実装対象外とする。

- 行動履歴を用いた**高度**なパーソナライズ
- 避難アシスト
- 複数都市・全国・海外展開
- 収益化機能
- 高度なリアルタイム処理基盤
- ヘルス情報を用いた判断
- アプリ起動後の自由入力による相談機能

### 3.4 将来構想・次段階以降

- 行動履歴を用いたパーソナライズ
- 避難アシスト
- ヘルス情報を用いた判断
- より高度なリアルタイムデータ処理

## 4. 想定利用者

### 4.1 主な利用者

東京都民および東京都内にいる個人利用者を主対象とする。

想定例:

- 通勤・通学する人
- 外出前にその日の注意点を知りたい人
- 観光やイベント参加などで都市情報に不慣れな人

### 4.2 利用環境

- iOSネイティブアプリ
- SwiftUIを利用した実装を前提とする
- スマートフォン利用を前提とする
- プッシュ通知の許可を前提とする
- 現在地情報利用の許可を前提とする
- iOSの対応範囲は開発チームが保有する端末を対象とする
- ヘルス情報はMVPでは利用しない

## 5. ユースケース

### 5.1 今日のアドバイス

利用者は、朝の設定時刻または日中の発火イベントが発生したタイミングで、その時点で必要な判断を確認できる。アプリ起動後の自由入力は行わず、設定画面で登録した情報と現在地周辺の都市データをもとに判断を生成する。

MVPでは、1回につき重要度の高い判断を1件表示する。

想定される判断例:

- 水筒を持って出る
- 10分早く出発する
- 折りたたみ傘を持つ
- 混雑しやすいエリアを避ける
- 屋外活動を控える
- 外出先で使えそうなFREE Wi-Fi候補を確認する
- 工事区間を避ける経路を確認する
- 地震発生時に気象庁・自治体の公式情報を確認する

### 5.2 避難アシスト（将来機能・MVP対象外）

災害発生時、利用者が現在地をもとに避難行動を確認できる機能として、将来実装する。

将来対応する災害種別は複数を想定する。データソースは東京都オープンデータおよび地震・水害等の災害関連APIを基本とし、必要に応じて火災、WBGT、避難所等のAPIを検討する。デモや検証では、実データの取得が難しい場合にモックデータを利用できるものとする。

災害情報および避難所情報を表示する場合は、情報の出典を表示する。

避難先の提案後は、利用者の位置情報をGoogle Maps等の外部地図サービスへ渡して遷移する方式を基本とし、アプリ内で高度なルート計算は行わない。

想定される判断例:

- 最寄りの避難所を提案する
- 避難所を選んだ理由を表示する
- Google Maps等の外部地図アプリに遷移する
- 現在地や災害種別に応じて避難行動の注意点を提示する

## 6. 機能要件

### 6.1 今日のアドバイス

判断と推奨行動は1対1の関係とし、1回の生成で1件の判断と推奨行動を返す。

#### 6.1.1 判断生成

アプリは、都市データと外部データをもとに、利用者にとって当日重要な行動提案を1件生成する。判断と推奨行動は1対1の関係とし、複数の候補を同時に表示しない。

判断に利用するデータのうち、天気情報は必須の考慮対象とする。WBGT、公衆トイレ、FREE Wi-Fi、工事、地震情報等は、シナリオの発火条件と利用者タグに応じて組み合わせる。1回の生成では最も重要な判断を1件に絞るが、MVPで実現するシナリオの種類や利用データ数は1種類に限定しない。

出力には以下を含める。

- 判断タイトル
- 推奨行動
- 判断理由
- 不確実性または注意点
- 参照した主なデータ種別
- 必要に応じた注意事項

#### 6.1.2 表示

アプリのホーム画面には、今日の判断を中心に表示する。

表示方針:

- 情報量を最小化する
- 行動提案を最も目立たせる
- 理由は短く表示する
- 詳細データの羅列は避ける
- 判断根拠の詳細は、必要に応じてアコーディオンで展開できる

#### 6.1.3 通知

利用者に対して、毎朝のアドバイスおよび日中の判断通知をプッシュ送信できる。プッシュ通知機能はMVPに含める。

通知内容は、行動に直結する短い文言とし、推奨行動を最初に理解できる構成とする。

通知間隔は、1時間、3時間、6時間から利用者が設定できる。朝の通知は利用者が設定した時刻に送信し、それ以降は設定された通知間隔および1日の通知上限数に従って送信する。

同一内容の重複通知や、利用者の行動に影響しない情報による通知は行わない。利用者が通知を許可しない場合の挙動は、MVPのデモでは通知許可済みの端末を対象とする。

通知頻度は、システム上の設定値だけでなく、利用者の性格診断に合わせた表現で選択できるものとする。

- 通知頻度 高：うっかりや
- 通知頻度 普通：わんぱく
- 通知頻度 低：まじめ

通知文言のニュアンスも設定できる。デフォルトでは、同じ判断が継続した場合に3回目の通知から文言のニュアンスを変える。追加の通知案内役として、以下のキャラクターを用意する。

- 電気ウナギ発電所の博士
- 近所のおせっかいおばちゃん
- ペンギンの交通整理員
- 何でも俳句にする人
- いつかくる宇宙人侵略に備える近藤さん

### 6.2 避難アシスト（将来機能・MVP対象外）

#### 6.2.1 現在地取得

アプリは、利用者の許可を得たうえで現在地を取得する。

現在地は、避難所検索や避難行動判断に利用する。

#### 6.2.2 避難先提案

アプリは、現在地と東京都オープンデータ等をもとに、利用者に適した避難先または避難行動を提案する。

出力には以下を含める。

- 推奨避難先
- 推奨理由
- 現在地からの距離または移動目安
- 注意事項
- 外部地図アプリへの遷移導線

#### 6.2.3 Google Maps連携

アプリは、必要に応じてGoogle Maps等の外部地図サービスに遷移できる。

将来機能でも、地図上の高度なルート計算をアプリ内で完結させるのではなく、専門サービスへの遷移を基本とする。

### 6.3 AI判断

AIは、取得した複数データを説明するだけではなく、利用者が取るべき行動を生成する。

AIの出力には、必ず以下を含める。

- プッシュ通知
  - 推奨行動（結論）
- ホーム画面
  - 判断
  - 推奨行動（結論）
  - 理由
  - 不確実性または注意点
  - 判断に利用した参照データ

AIの回答（推奨行動）は、プッシュ通知に収まる文量、即座に理解できる内容であること。

AIの回答トーンは、平常時にやさしく安心感のある表現を基本とする。通知文言のキャラクター設定を選択した場合も、判断内容、推奨行動、理由の正確性を損なわない範囲で表現を変える。

AIへの指示はプロンプトを中心に設計し、MVPでは出力形式と判断ルールを明示する。禁止表現の網羅的な検査や高度な安全性検証は最小限とし、デモで利用するデータとシナリオは事前に確認する。

### 6.4 判断ルールと発火処理

AIエージェントがオープンデータを解釈し、出力する内容を決める際の考え方を判断ルールとして定義する。AIエージェントは、取得したデータを無制限に解釈するのではなく、以下の条件をもとに、利用者に提示すべき判断と推奨行動を選択する。

- オープンデータに重要な変化がある
- 利用者のタグに関連する
- 利用者の現在地または対象エリアに関係する
- 利用者の行動に影響する基準値を超えている
- 同一内容をすでに通知していない
- 変化によって推奨行動が変わる可能性がある

バッチ処理では、天気・交通・地域情報などの最新データを取得し、判断ルールに該当する場合にAIが判断、推奨行動、理由、参考情報を生成する。生成した内容は判断履歴に追加し、発火イベントが発生した場合に、未通知かつ通知条件を満たす履歴からプッシュ通知を送信する。

発火イベントは、朝の設定時刻および設定された通知間隔の経過とする。朝の通知以降は、1日の通知上限数を超えて送信しない。

### 6.5 判断履歴

AIが生成した判断、推奨行動、理由、参照データ、生成日時、通知状態を判断履歴として保存する。

判断履歴の表示には匿名IDを利用する。履歴として保存されたAIの回答は、次回のAI判断における情報源としては利用しない。

### 6.6 データ取得・統合

システムは、東京都オープンデータおよび外部APIから必要なデータを取得し、AI判断に利用できる形式に整形する。

MVPでは、天気を必須データとして扱う。その他のデータは、判断への関連性と取得可能性に応じて組み合わせる。シナリオ、取得URL、API URLの台帳は基本設計書の6.4〜6.9で管理する。

MVPで扱うデータ:

- 天気
- WBGT
- 公衆トイレの位置情報（補助）
- 東京都 公衆無線LANアクセスポイント一覧
- 23区内 都道の路上工事情報
- 気象庁 地震情報一覧JSON（公式情報確認の注意喚起に限定）

追加候補（未採用または要件確認中）:

- 公共交通
- 混雑
- PM2.5等の大気環境

将来機能で扱うデータ候補:

- 避難所
- 災害関連情報

## 7. 非機能要件

### 7.1 パフォーマンス

通常利用時は、利用者がストレスなく判断を確認できる応答速度を目指す。

MVPでは、バッチで生成済みの判断を履歴から取得して通知・表示することで、利用者が待たずに確認できることを優先する。高度なリアルタイム処理基盤は将来課題とする。

### 7.2 可用性

Cloudflareのマネージドサービスを活用し、MVPとして十分な可用性を確保する。

### 7.3 セキュリティ

位置情報を扱うため、必要最小限のデータ取得・保存に限定する。

APIキーやAI連携情報は、クライアントアプリ内に直接保持しない。

### 7.4 プライバシー

位置情報の取得目的を明確にし、ユーザーの許可を得た場合のみ利用する。位置情報は端末内に保存し、AI判断や周辺データ取得に必要な場合のみ一時的に利用する。クラウド上に位置情報を永続保存しない。

ユーザーアカウントは作成せず、判断履歴の表示に必要な匿名IDのみを利用する。行動履歴およびヘルス情報はMVPでは保存・利用しない。

### 7.5 説明可能性

AI判断には、なぜその判断になったのかを説明する理由を必ず含める。

判断根拠として利用したデータ種別を可能な範囲で表示する。

通知からアプリを開いた後、詳細根拠はアコーディオン形式で確認できるようにする。

## 8. 技術要件

### 8.1 クライアント

- iOSネイティブアプリ
- SwiftUI
- 位置情報取得
- プッシュ通知
- 外部地図アプリ連携
- 開発チームが保有する端末を対応対象とする

### 8.2 バックエンド

Cloudflareの利用を前提とする。MVPではCloudflare Workersを中心に構成し、要件に応じて他サービスへ拡張する。具体的な構成は基本設計で確定する。

利用候補:

- Cloudflare Workers
- Cloudflare Workers KV
- Cloudflare D1
- Cloudflare R2
- Cloudflare Queues
- Cloudflare Cron Triggers
- Cloudflare AI Gateway
- Cloudflare Cache

### 8.3 AI

- Cloudflare Workers AIを利用する
- AIへの入力には、オープンデータおよび現在地周辺情報を含める
- AIの出力は、アプリ表示に適した構造化データとして扱う

### 8.4 外部サービス

- 東京都オープンデータ
- 天気・気象・災害関連API
- Google Maps等の外部地図サービス
- 必要に応じた公共交通・混雑・イベント・大気環境関連API

### 8.5 AIへ渡すデータ

オープンデータは、検索・統合・AIからの参照を高速化するため、必要なデータをDBに取り込む。CSVやTSV等のデータをそのままAIに渡さず、検索しやすい形式に整形する。

リアルタイム性が求められるデータは、外部サービスのAPIから取得して利用する。MVPでは、リアルタイム取得データをDBに永続保存せず、将来的にキャッシュを導入する。

AIが生成した判断結果は履歴としてDBに保存する。ただし、過去のAI回答を次回の判断における情報源としては利用しない。

## 9. 想定システム構成

```text
iOS App
  |
  | HTTPS API
  v
Cloudflare Workers
  |
  |-- 東京都オープンデータ取得
  |-- 外部API取得
  |-- バッチ / Cronによる定期データ取得
  |-- キャッシュ / DB
  |-- Cloudflare Workers AI 連携
  |
  v
AI判断結果 / 判断履歴
  |
  ├-- iOS Appで表示
  └-- 発火イベントに応じたプッシュ通知
```

## 10. 画面要件

### 10.0 基準画面構成
- ホーム画面
- 設定（ハンバーガーメニューから出るサイドバー）
- 履歴（ハンバーガーメニューから出るサイドバー）

### 10.1 ホーム画面

ホーム画面では、今日の判断を中心に表示する。

表示項目:

- 今日の判断
- 推奨行動
- 理由
- 不確実性または注意点
- 判断に利用した参照データ
- 必要な補足情報（参考情報のリンク等）

### 10.2 避難アシスト画面（将来機能・MVP対象外）

避難アシスト画面では、現在地をもとにした避難判断を表示する。

表示項目:

- 推奨避難先
- 推奨理由
- 距離または移動目安
- 注意事項
- Google Maps等への遷移ボタン

### 10.3 設定画面

MVPでは最小限の設定画面を想定する。

表示項目:

- 位置情報利用に関する案内
- 通知許可に関する案内
- ユーザータグまたは基本属性の設定
- 朝の通知時刻
- 通知間隔（1時間、3時間、6時間）
- 1日の通知上限数
- 通知頻度（うっかりや、わんぱく、まじめ）
- 通知文言のニュアンス（デフォルト、案内役）
- 利用規約・プライバシーポリシーへの導線

アプリ利用登録や約款等の導線は、商用リリースを視野に入れた段階で別途検討する。MVPでは、アプリ起動後の自由入力による相談画面は設けない。

### 10.4 履歴画面

履歴画面では、匿名IDに紐づく過去の判断を表示する。

表示項目:

- 判断日時
- 判断
- 推奨行動
- 判断理由
- 参照データ
- 通知済みかどうか

## 11. 成功条件

MVPの成功条件は、以下とする。

- プッシュ通知で、70~80%程度の利用者が内容を理解できる
- 利用者がアプリを開いたとき、今日取るべき行動を短時間で理解できる
- 1回の判断につき、重要度の高い推奨行動を1件表示できる
- 今日のアドバイスが、複数データを統合した判断として提示される
- AIの回答が情報羅列ではなく、行動提案になっている
- 判断理由が利用者に理解できる
- iOSアプリ、Cloudflareバックエンド、Cloudflare Workers AIが一連の流れとして接続される
- 実データを取得し、判断履歴への保存とプッシュ通知までの一連の体験が成立する

## 12. リスク・制約

### 12.1 データ更新頻度の差

東京都オープンデータや外部APIは、形式や更新頻度が異なる可能性がある。

MVPでは、利用データを一律に絞り込むのではなく、シナリオごとに必要なデータをデータパックとして管理する。各データは、取得方法、鮮度、対象エリア、ライセンス、出典表示を確認したうえで判断に利用する。

データ統合方針の詳細は、基本設計以降で確定する。

### 12.2 災害時利用の責任（将来機能に関する留意事項）

避難アシストは将来、災害時の判断支援を目的として実装する。ただし、実災害での安全性を保証する機能として提供するには、専門機関の情報を優先する設計や十分な検証が必要となる。

免責表示や注意喚起が必要である。

### 12.3 AI判断の誤り

AIが不正確な判断を生成する可能性がある。

判断理由、参照データ、不確実性を表示し、利用者が過信しない設計が必要である。

### 12.4 通知過多

通知が多すぎると利用者のストレスにつながる。

通知間隔、性格診断に基づく通知頻度、1日の通知上限数を設定できるようにする。同一内容の重複通知や、行動に影響しない情報による通知を抑制する。

### 12.5 データのライセンスと出典

MVPで利用するデータは、開発チームが準備し、採用前に利用規約、ライセンス、取得方法、出典表示の要否を確認する。確認内容はチーム内で相互レビューする。

AIの判断結果には、可能な範囲で判断に利用したデータ種別または参考情報を表示する。

### 12.6 外部サービス障害

外部API、東京都オープンデータ、Cloudflare Workers AIのいずれかが利用できない場合に備え、デモでは事前に確認したデータまたはモックデータへ切り替えられるようにする。

デモ中にデータ取得やAI判断が成立しない場合は、エラー画面だけを表示せず、TOKYO+の世界観に沿ったミニゲームを表示する。ミニゲームはMVPのデモ演出として必須とする。

### 12.7 権限未許可時の扱い

位置情報が許可されない場合は、位置情報を必要とする判断を生成せず、利用者に許可が必要であることを案内する。通知が許可されない場合はプッシュ通知を送信せず、MVPのデモは通知許可済みの端末を対象とする。

### 12.8 デモ運用

デモ期間中の障害対応者は事前に固定せず、データ/API障害、アプリ障害、デモ判断を含め、発生時にチーム内で対応する。

## 13. デモ・受入条件

### 13.1 デモ内容

- デモ対象エリアは東京駅周辺を主エリアとし、東京都全域データを組み合わせる
- 今日のアドバイスは、暑さ、天候変化、外出先通信、工事、地震情報確認の複数シナリオを実演対象とする
- 複数データをAIが統合し、判断、推奨行動、理由、参考情報を返す体験を中心に見せる
- デモで利用するデータとシナリオは事前に確認し、AIの表示内容を検証しておく
- デモ動画を作成し、デモURLからTestFlightを共有する
- API付きプロトタイプを提出対象とする

### 13.2 受入シナリオ

以下の複数シナリオと共通障害シナリオでMVPの受入確認を行う。

1. 正常系：実データを取得し、AI判断、履歴保存、プッシュ通知、アプリ表示まで確認する
2. 暑さ・屋外移動：気象庁、WBGT、公衆トイレデータを統合して休憩地点確認の提案を生成する
3. 外出先通信：東京都FREE Wi-Fiデータから東京駅周辺の候補を抽出し、設置場所の確認を提案する
4. 工事・経路：東京駅周辺の当月工事区間を抽出し、迂回経路の確認を提案する
5. 地震情報：基準以上の地震情報がある場合だけ、公式情報の確認を提案する。避難先は独自に断定しない
6. 位置情報拒否：位置情報を必要とする判断が停止し、利用者への案内が表示されることを確認する
7. 通知拒否：プッシュ通知が送信されず、アプリ内表示は継続することを確認する
8. 外部API障害：モックまたは事前確認済みデータへ切り替わり、必要に応じてミニゲームが表示されることを確認する

### 13.3 将来機能の確認事項

避難アシストはMVPの受入対象には含めず、将来構想として要件を保持する。将来実装時には、複数の災害種別、災害関連API、避難所データ、現在地を用いた避難先の選定、Google Maps等への遷移を別途検証する。

ヘルス情報の利用も将来構想として残し、利用目的、取得項目、同意、保存期間を別途定義する。

## 14. 決定事項一覧

本書では、以下の方針を確定事項として扱う。

- MVPの対象は「今日のアドバイス」のみとする
- ハッカソンの中心体験は、複数データをAIが統合し、理由付きの判断と推奨行動を返すこととする
- 天気情報は判断に必ず考慮する
- 判断は1回につき1件表示する
- 実データ連携とプッシュ通知をMVPに含める
- ユーザー入力は設定画面に限定し、アプリ起動後の自由入力は扱わない
- 位置情報は同意のうえ端末内で管理し、クラウドに永続保存しない
- ユーザーアカウントは作成せず、匿名IDで判断履歴を管理する
- AIへの入力はオープンデータおよび現在地周辺情報を中心とする
- 行動履歴およびヘルス情報は将来構想とする
- Cloudflare Workersを中心に構成し、具体的なインフラ構成は基本設計で確定する
- iOSアプリはSwiftUIで実装し、開発チームが保有する端末を対象とする
- 避難アシストは将来機能として保持する
